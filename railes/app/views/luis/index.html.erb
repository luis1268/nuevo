<p id="notice"><%= notice %></p>

<h1>Luis</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Apellido</th>
      <th>Edad</th>
      <th>Email</th>
      <th>Contraseña</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @luis.each do |lui| %>
      <tr>
        <td><%= lui.name %></td>
        <td><%= lui.apellido %></td>
        <td><%= lui.edad %></td>
        <td><%= lui.email %></td>
        <td><%= lui.contrasena %></td>
        <td><%= link_to 'Show', lui %></td>
        <td><%= link_to 'Edit', edit_lui_path(lui) %></td>
        <td><%= link_to 'Destroy', lui, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lui', new_lui_path %>
